[tool.poetry]
name = "envio_email_capacitacao"
version = "0.1.0"
description = ""
authors = ["Ricardo Ferreira"]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
